namespace Hacknet
{
    public struct VehicleType
    {
        public string model;
        public string maker;

        public VehicleType(string vModel, string vMaker)
        {
            model = vModel;
            maker = vMaker;
        }
    }
}